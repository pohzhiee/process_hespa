cmake_minimum_required(VERSION 3.7)
project(process_hespa)
SET(CMAKE_INCLUDE_PATH ${CMAKE_INCLUDE_PATH} "C:/Users/Win10/Desktop/boost_1_64_0")
SET(CMAKE_LIBRARY_PATH ${CMAKE_LIBRARY_PATH} "C:/Users/Win10/Desktop/boost_1_64_0/stage/lib")
set(CMAKE_CXX_STANDARD 11)
set(CMAKE_CXX_FLAGS "-fopenmp")
set(Boost_USE_FILESYSTEM_LIBS ON)
set(SOURCE_FILES main.cpp)

find_package(Boost 1.64.0 COMPONENTS filesystem system)
if(Boost_FOUND)
    include_directories(${Boost_INCLUDE_DIRS})
    add_executable(process_hespa ${SOURCE_FILES})
    target_link_libraries(process_hespa ${Boost_LIBRARIES} -static-libstdc++ -static-libgcc)
endif()
